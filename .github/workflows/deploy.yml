name: Deploy to GitHub Pages

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      pages: write
      id-token: write
    
    environment:
      name: github_pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Install PNPM
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install

      # - run: npx nuxt build --preset github_pages

      # - name: Upload artifact
      #   uses: actions/upload-pages-artifact@v1
      #   with:
      #     path: ./.output/public

      # - uses: actions/checkout@v3
      #   with:
      #     fetch-depth: 0
    
      - name: Setup Git identity
        run: |
          git config --local user.email "masterupvcup@gmail.com"
          git config --local user.name "Master upv cup"

      # - name: Deploy to GitHub Pages
      #   id: deployment
      #   uses: actions/deploy-pages@v1
    
    # - name: Add GitHub Token for Authentication
    #   run: git remote set-url origin https://${{ secrets.GITHUB_TOKEN }}@github.com/masterupvcup/website.git

      - name: Deploy
        run: pnpm run deploy
        # run: npx gh-pages --dotfiles -d .output/public -u "GitHub Actions <actions@github.com>" --repo https://${{ secrets.GITHUB_TOKEN }}@github.com/masterupvcup/website.git
        # env:
          # Set any environment variables needed for deployment
          # For example:
          # MY_SECRET: ${{ secrets.MY_SECRET }}
